{
  "name": "aws-bedrock-multiagents",
  "private": true,
  "packageManager": "pnpm@10.6.4",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist cdk.out"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^22.13.10",
    "typescript": "^5.7.3"
  }
}
